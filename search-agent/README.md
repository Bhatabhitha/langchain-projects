# search-agent

Agentic search project (LangChain).

## Setup

From the repo root, with the shared venv activated:

```bash
pip install -e ./search-agent
python search-agent/main.py
```

Copy `.env.example` to `.env` and fill in your keys.
